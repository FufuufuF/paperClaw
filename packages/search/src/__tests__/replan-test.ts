/**
 * Archived replan test.
 *
 * The old pipeline entry (`flows/query-flow.ts`) was removed when the project
 * switched to the nanobot-style agent base. Keeping this file as a no-op marker
 * documents why the old test is no longer executed while preserving the place
 * where a new `paper_search` tool replan test should land.
 *
 * TODO: Rebuild this test after `packages/search/src/paper-search-tool.ts` exists.
 */
export {};
