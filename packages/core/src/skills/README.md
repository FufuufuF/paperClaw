# paperClaw skills

Skill files are markdown instructions loaded into the master agent context.

This mirrors nanobot's `skills/<name>/SKILL.md` convention: code provides tools,
while skills tell the agent when and how to use those tools.
